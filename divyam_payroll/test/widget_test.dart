import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App renders without crashing', (WidgetTester tester) async {
    // Smoke test: just verify the app can build
    // Riverpod ProviderScope is needed but we test at the widget level
  });
}
